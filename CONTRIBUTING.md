# 贡献指南

感谢你对 MacServerMonitor 的关注！我们欢迎各种形式的贡献。

## 🤝 如何贡献

### 报告问题

如果你发现了 bug 或有功能建议：

1. 在 [Issues](https://github.com/SepineTam/MacServerMonitor/issues) 中搜索是否已有类似问题
2. 如果没有，创建新的 Issue
3. 提供详细的信息：
   - macOS 版本
   - MacServerMonitor 版本
   - 重现步骤
   - 预期行为 vs 实际行为
   - 截图（如果相关）

### 提交代码

#### 开发环境设置

```bash
# 克隆仓库
git clone https://github.com/SepineTam/MacServerMonitor.git
cd MacServerMonitor

# 构建项目
swift build

# 运行项目
swift run
```

#### 代码规范

请遵循 [CLAUDE.md](CLAUDE.md) 中定义的开发指南：

- **命名规范**：
  - 类名/结构体：大驼峰（`DeviceManager`）
  - 方法/属性：小驼峰（`deviceName`）
  - 常量：小驼峰或全大写

- **注释规范**：
  - 函数注释：英文，使用 `///`
  - 行内注释：中文可以使用
  - 使用 `// MARK:` 分隔代码区域

- **Commit Message**：
  ```
  <type>: <description>
  ```
  类型：`feat`, `fix`, `refactor`, `perf`, `docs`, `style`, `test`, `chore`

  示例：
  ```
  feat: add device discovery
  fix: resolve memory leak in metrics collection
  docs: update README with new features
  ```

  **禁止**：
  - ❌ 不要添加 "Co-Authored-By"
  - ❌ 使用英文写 commit message

#### 开发流程

1. **Fork 仓库**
2. **创建分支**：
   ```bash
   git checkout -b feature/your-feature-name
   ```

3. **开发和测试**：
   ```bash
   # 本地测试
   swift build
   ./build.sh test
   ```

4. **提交代码**：
   ```bash
   git add .
   git commit -m "feat: description of your changes"
   ```

5. **推送到你的 Fork**：
   ```bash
   git push origin feature/your-feature-name
   ```

6. **创建 Pull Request**：
   - 在 GitHub 上打开 PR
   - 填写 PR 模板
   - 等待代码审查

#### Pull Request 检查清单

在提交 PR 前，请确保：

- [ ] 代码构建成功
- [ ] 遵循代码规范
- [ ] 添加了必要的注释
- [ ] 更新了相关文档（如需要）
- [ ] 测试了新功能
- [ ] Commit message 格式正确
- [ ] 没有引入新的编译警告

## 🎯 优先贡献的功能

如果你正在寻找可以贡献的方向，以下是一些不错的起点：

### 初级（适合新手）

- [ ] 添加更多单元测试
- [ ] 改进错误消息
- [ ] 优化 UI 布局
- [ ] 添加键盘快捷键
- [ ] 完善文档

### 中级

- [ ] 性能优化
- [ ] 实现数据可视化图表
- [ ] 添加自定义主题
- [ ] 改进设备发现机制
- [ ] 添加数据导出功能

### 高级

- [ ] 实现 mDNS/Bonjour 设备发现
- [ ] 添加设备分组功能
- [ ] 实现插件系统
- [ ] 添加 Widget 支持
- [ ] 实现 AI 预测功能

详见 [ROADMAP.md](ROADMAP.md)

## 📝 开发资源

### 重要文档

- [CLAUDE.md](CLAUDE.md) - 开发指南（项目"宪法"）
- [ROADMAP.md](ROADMAP.md) - 产品路线图
- [CHANGELOG.md](CHANGELOG.md) - 版本历史
- [README.md](README.md) - 项目说明

### 技术栈

- **语言**: Swift 5.9+
- **框架**: SwiftUI
- **系统**: macOS 13.0+
- **架构**: MVVM + 单例模式
- **包管理**: Swift Package Manager

### 项目结构

```
MacServerMonitor/
├── Core/              # 核心逻辑
│   ├── Alerts/       # 告警系统
│   ├── Device/       # 设备管理
│   ├── Sampling/     # 数据采样
│   ├── Settings/     # 设置存储
│   ├── Store/        # 指标存储
│   └── Theme/        # 主题管理
├── UI/               # 用户界面
│   ├── Alerts/       # 告警界面
│   ├── Dashboard/    # 主界面
│   ├── Devices/      # 设备管理
│   └── Settings/     # 设置界面
├── Utils/            # 工具类
└── Resources/        # 资源文件
```

## 🐛 调试技巧

### 查看日志

应用会在控制台输出日志：
```swift
print("[Debug] Message")
```

### 常见问题

1. **编译错误**
   - 确保 macOS 版本 >= 13.0
   - 运行 `swift package resolve` 更新依赖

2. **权限问题**
   - 应用不需要特殊权限
   - 如果遇到权限问题，检查是否有网络访问

3. **设备发现失败**
   - 确保设备在同一局域网
   - 检查防火墙设置
   - 验证 HTTP 服务器是否启用

## 💬 交流方式

- **Issues**: [GitHub Issues](https://github.com/SepineTam/MacServerMonitor/issues)
- **Discussions**: [GitHub Discussions](https://github.com/SepineTam/MacServerMonitor/discussions)
- **Email**: sepinetam@gmail.com

## 📄 许可证

贡献的代码将使用 [MIT License](LICENSE) 发布。

---

**感谢你的贡献！** 🎉
